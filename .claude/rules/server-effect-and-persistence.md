# Rule: server Effect conventions, buses, persistence & auth

**Scope:** `packages/server/` — read before writing handlers, wiring layers, publishing events, or touching SQL/auth.
**Backing ADRs:** 0004 (errors as tagged errors), 0006 (typed bus), 0007 (unit of work + event buses), 0012 (observability/spans), 0016–0017 (auth via Zitadel BFF), 0020 (per-module DB schemas).

- **Errors as `Schema.TaggedErrorClass`** (ADR-0004; `Schema.TaggedError` in effect v3). Domain errors live in `domain/`, contract errors live in the contracts package, and the HTTP endpoint maps domain → contract via `Effect.catchTag`.
- **Typed bus** (ADR-0006). `bus.execute(SomeCommand, {...})` returns the exact `Effect<A, E, never>` the message's own definition declares — the definition is the single source of truth, so there is no registry to keep in sync. No casts, no `Result` unwrapping. Messages are declared with `Command.make`/`Query.make` from `@org/cqrs`; each definition publishes a `<Name>Payload` type its handler names. Dispatchers are per-module surfaces (`XCommands`/`XQueries`) composed in dependency order at the composition root, so a handler that reaches another module resolves against that module's surface instead of the whole bus.
- **Unit-of-work boundary + two buses** (ADR-0007). Command handlers declare the transaction once at the boundary with `.pipe(withUnitOfWork)` (not an inner `uow.run` block); it demotes `DatabaseError` to a defect and surfaces `PersistenceUnavailable`. `UnitOfWork.run` is the low-level escape hatch (integration tests, sub-blocks that keep external IO outside the tx). Nested runs open a real **savepoint**: a caught nested failure rolls back only the savepoint while the outer commits. Two domain-event buses share one `DomainEvent` base — the bus you publish to is the switch: **`DomainEventBus`** (immediate; subscribers run in the publisher's fiber/transaction, a failure rolls the publisher back) vs **`IntegrationEventBus`** (eventual; `dispatch` buffers, the outermost `run` flushes after commit, each handler in its own transaction with failures logged/isolated). Default new cross-aggregate reactions to the integration bus. Dispatching to either bus outside a unit of work is a defect (fail-fast on a forgotten `withUnitOfWork`). In-memory flush for now (lossy on commit-then-crash); transactional outbox is the deferred durability follow-up.
- **Ports vs Lives** (ADR-0007). The DDD shared kernel lives in `platform/ddd/` as ports, tiered into two folders (ADR-0008): `platform/ddd/contracts/` holds the domain-safe _types/contracts_ (`DomainEvent`, `SpanAttributesExtractor`, `PersistenceUnavailable`, `PostCommitBuffer`) the domain may reference; `platform/ddd/ports/` holds the application-tier _services_ (`UnitOfWork`, `DomainEventBus`, `IntegrationEventBus` — and no cross-module ACL services; those are consumer-owned per ADR-0022) plus `eventSpanAttributes` and the `withUnitOfWork` combinator. The rule: domain may depend on contracts, never on ports. Production implementations live in sibling `platform/*-live.ts` files (`UnitOfWorkLive`, `makeDomainEventBusLive`, `makeIntegrationEventBusLive`). Use cases, interface, and middlewares depend on `platform/ddd/` only; Lives are wired at the composition root (`server.ts`, `test-utils/test-server.ts`) and in integration tests that intentionally stage a sub-graph. Enforced by `lives-only-from-composition-roots`; the contracts/ports tiering is enforced by `domain-isolation` + `ddd-contracts-no-ports`.
- **The command/query buses come from `@org/cqrs`, not `platform/ddd/`** (ADR-0006). Import `CommandBus` / `QueryBus` (the Tags) from `@org/cqrs`. A module publishes its own slice with `Command.dispatcher(group, { spanAttributes })` in `<feature>.command-handlers.ts` and exports it as a Tag from its barrel. `makeCommandBus` / `makeQueryBus` / `mergeDispatchTables` fold those into one routing table and are restricted to the composition roots by an eslint `no-restricted-imports` entry — a path rule can't reach them through the package barrel.
- **Spans: boundary + use-case** (ADR-0012). Spans live at the command/query/event bus and at HTTP endpoints, plus a use-case span per handler declared with `Effect.fn("<handlerName>")` — instrument at use-case granularity, no finer. HTTP/CLI **endpoint adapters** also declare their boundary span via `Effect.fn("<GroupLive.op>")` (not an explicit `Effect.withSpan`) — the effect-v4 idiom, and what `@effect/language-service`'s `effectFnOpportunity` steers toward. Span attributes are sibling extractor functions composed at registration.
- **Effect diagnostics gate** (`pnpm check:effect`, part of `check:all`). `@effect/language-service` runs as a tsconfig plugin (in-editor) and in CI via `scripts/check-effect-diagnostics.mjs`, which fails on ANY finding. Keep it green: prefer `yield* someError` over `yield* Effect.fail(someError)` for yieldable (`Schema.TaggedErrorClass`) errors, declare handlers/endpoints with `Effect.fn`, and merge chained `Effect.provide(a), Effect.provide(b)` into one `Effect.provide(Layer.mergeAll(a, b))` (or `x.pipe(Layer.provideMerge(y))` when `y` satisfies `x`). Per-rule severities live in `tsconfig.base.json`'s plugin config — turn a rule `off` there **only** when it genuinely doesn't apply (e.g. `preferSchemaOverJson` is off: every JSON boundary here is a deliberate wire/test seam), never to dodge a real fix.
- **Effect v4 baseline.** Pinned to `effect@4.0.0-beta.94` (exact — `effect/unstable/*` may break on beta bumps). Domain result idiom is `effect/Result` (not `Either`); errors are `Schema.TaggedErrorClass`; services are `Context.Service<Self, Shape>()("Id")` with an explicit `Layer`. HTTP moved to `effect/unstable/httpapi` + `effect/unstable/http`. Two v4 gotchas contributors hit: `HttpApiClient` encodes a `Schema.Class` payload/params/query **strictly** — construct the contract class at the call site (`new SomeContract.CreatePayload({...})`), a plain object throws at runtime; and a service a module owns but an endpoint consumes (OIDC client, invitation mailer, billing gateway) must be published as a bundled layer and provided **post-serve** at the composition root — `HttpRouter.provideRequest` type-checks but does not reach `HttpApiBuilder`'s routes at runtime.
- **Authentication via self-hosted Zitadel as a server-side BFF** (ADR-0016, ADR-0017). The SPA never holds access or id tokens; the server runs the OIDC dance and issues a `HttpOnly` session cookie. Application code consumes `CurrentUser` (`@org/contracts/Policy`); only `modules/auth/` and `platform/auth/` know about Zitadel. Roles live app-side in `users.role`; the seed script pre-seeds the admin's `users` + `auth_identities` rows so the first sign-in finds an existing identity.
- **Per-module DB schemas** (ADR-0020). Each module owns a Postgres schema named after its folder — `user`, `organization`, `todos`, `wallet`, `auth`, `billing` — plus a shared `platform` schema for cross-cutting tables (the `role` module's data lives in `platform.roles`). Application SQL must address tables by their owning schema — `"user".users`, `todos.todos`, `wallet.wallets`, `auth.auth_identities`, `auth.sessions`, `platform.roles`. The `"user"` quotes are required (Postgres reserved word). Cross-schema FKs are allowed at DDL only (`wallet.wallets.user_id → user.users.id`, both `auth.*.user_id → user.users.id`); application SQL must never JOIN, SELECT, INSERT, UPDATE, or DELETE across schemas. Cross-module reads continue to flow through the synchronous event bus + interface/events ACL. Enforced by `@synapsestudios/data-boundaries/no-cross-schema-slonik-access` in `eslint.config.mjs` (scoped to `packages/server/src/modules/**`, excluding test files). Adding a module: write a `Vxxx__create_schema_<name>.sql` migration and add `<name>` to `MODULE_SCHEMAS` in all three test-database files (`packages/server/src/test-utils/test-database.ts`, `packages/jobs/src/test-utils/test-database.ts`, `packages/acceptance/test-utils/database.ts`).
